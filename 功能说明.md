# A29电动门控制系统功能说明文档

## 项目概述

本项目是一个基于TX8C126x单片机的电动门控制系统，主要用于控制电动门的开关操作。系统通过检测传感器信号和按键输入，实现电动门的自动开启和关闭，并具备过流保护功能。

## 系统架构

### 硬件模块
- **主控芯片**: TX8C126x 单片机
- **GPIO配置**: 用于电机控制和传感器检测
- **定时器**: 用于任务调度和中断处理
- **ADC**: 用于检测电流和电机位置信息
- **PWM**: 用于电机速度控制

### 软件模块
- **Main模块**: 系统初始化和主循环
- **System模块**: 系统时钟和基本配置
- **Timer模块**: 定时器中断和任务管理
- **Task模块**: 任务调度系统
- **GPIO配置模块**: 端口配置和电机控制


## GPIO引脚分配

- 源工程的引脚：
  
| 引脚编号 | 引脚 | 功能                  | 说明                                  |
| -------- | ------ | --------------------- | ------------------------------------- |
| 2        | P31  | 自锁开关输入          | 检测用户操作信号                      |
| 3        | P06  | 电机反转PWM           | 通过STIMER1输出PWM信号控制电机反转    |
| 4        | P05  | 后向位置检测/电流检测 | ADC输入，检测门是否回到原始位置       |
| 5        | P22  | 前向过流检测          | ADC输入，检测前向运动时是否遇到障碍物 |
| 6        | P21  | 电机正转PWM           | 通过STIMER0输出PWM信号控制电机正转    |
- 在demo板上测试用的引脚:
  



## 功能模块详述

### 1. 系统初始化 
```
system_init
```
- 配置系统时钟为HIRC 48MHz
- 设置FLASH写入时钟为1MHz
- 初始化I/O功能映射
- 配置看门狗

### 2. 定时器模块
```
timer0_init
```
- 配置Timer0为周期中断模式
- 设置中断频率以支持任务调度
- 在中断服务程序中调用任务调度函数

### 3. 任务调度模块
系统采用多任务调度机制，包含以下任务：

#### 3.1 电机控制任务 
```
MotorHadnler
```
- **执行周期**: 每1ms执行一次
- **功能描述**:
  - 控制电机正转(`MOTOR_FORWARD`) 、反转(`MOTOR_BACKWARD`)或停止(`MOTOR_STOP`)
  - 通过PWM控制电机速度
  - 实现电机状态转换时的延时（300ms）
  - ADC检测电流，实现过流保护
  - 防止工作时间过长的强制复位功能

#### 3.2 按键处理任务 
```
keyHandler
```
- **执行周期**: 每5ms执行一次
- **功能描述**:
  - 检测自锁开关状态变化
  - 检测到上升沿时启动电机正转
  - 实现边沿触发，避免按键抖动问题

## 电机控制逻辑

### 电机状态转换
1. **初始状态**: `MOTOR_STOP`
2. **按键触发**: 检测到自锁开关上升沿，切换到`MOTOR_FORWARD`
3. **过流检测**: 检测到前向过流，切换到`MOTOR_BACKWARD`
4. **位置检测**: 检测到后向位置，切换到`MOTOR_STOP`
### PWM控制
- 使用STIMER0控制正转PWM信号
- 使用STIMER1控制反转PWM信号
- PWM频率设置为20kHz
- 驱动占空比设置为85%（值为2040）

### 保护机制
1. **过流保护**: 当ADC检测值超过1657（对应1.7V）时，认为发生过流
2. **工作时间限制**: 防止电机工作时间超过2500个周期（约2.5秒）
3. **状态切换延时**: 电机状态切换时延时300ms，防止快速切换损坏电机

                



## ADC检测功能
测试时暂时使用demo板上的4个引脚
```

P26 adc0 forward
P25 adc0 reverse
P24 adc1 forward    
P23 adc1 reverse

```


## 安全特性

1. **看门狗**: 主循环中定期喂狗，防止程序跑飞
2. **过流保护**: 检测到过流时立即切换电机方向或停止
3. **时间限制**: 防止电机长时间工作导致过热
4. **状态延时**: 避免电机状态快速切换
5. **故障安全**: 发生故障时将电机控制信号拉低，确保电机停止

## 工作流程

1. 系统上电后初始化各模块
2. 检测到自锁开关上升沿，电机开始正转
3. 正转过程中持续检测前向过流信号
4. 检测到前向过流或达到最大工作时间，电机切换到反转
5. 反转过程中检测原始位置信号
6. 检测到原始位置，电机停止
7. 等待下次按键操作

此电动门控制系统具备完善的安全保护机制，能够自动检测位置和过流情况，确保门的正常开关操作并防止损坏。