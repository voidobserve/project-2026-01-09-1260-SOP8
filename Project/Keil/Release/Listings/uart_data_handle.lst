C51 COMPILER V9.60.7.0   UART_DATA_HANDLE                                                  02/11/2026 10:29:10 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE UART_DATA_HANDLE
OBJECT MODULE PLACED IN .\Release\Objects\uart_data_handle.obj
COMPILER INVOKED BY: C:\Keil_v5\C51\BIN\C51.EXE ..\..\User\uart_data_handle.c LARGE OPTIMIZE(8,SPEED) BROWSE INTVECTOR(0
                    -X000C) INCDIR(..\..\Libraries\Include;..\..\User;..\..\Hardware) INTERVAL(3) DEBUG OBJECTEXTEND PRINT(.\Release\Listings
                    -\uart_data_handle.lst) OBJECT(.\Release\Objects\uart_data_handle.obj)

line level    source

   1          #include "uart_data_handle.h"
   2          #include "motor_handle.h"
   3          #include "user_config.h"
   4          
   5          static volatile u8 cmd_buff[10] = {0};    // 存放接收到的一条指令
   6          static volatile u8 cur_cmd_buff_len = 0;  // 指示当前接收到的指令的索引（之后会在程序�
             -��更新，不用清零）
   7          static volatile u8 dest_cmd_buff_len = 0; // 存放最终要接收的指令长度（之后会在程序中�
             -��新，不用清零）
   8          
   9          static volatile u8 timeout_enable = 0; // 超时计数使能
  10          static volatile u16 timeout_cnt = 0;   // 超时计数
  11          
  12          // 由定时中断调用，累计超时计数
  13          void uart_data_recv_timeout_add(void)
  14          {
  15   1          if (timeout_enable)
  16   1          {
  17   2              timeout_cnt += 10; // 有计数溢出的风险，要注意在溢出前进行处理
  18   2          }
  19   1      }
  20          
  21          // void uart_data_recv_timeout_clear(void)
  22          // {
  23          //     timeout_cnt = 0;
  24          // }
  25          
  26          void uart_data_handle(void)
  27          {
  28   1          static volatile u8 uart_data_handle_status = UART_DATA_HANDLE_STATUS_IDLE; // 状态机
  29   1      
  30   1          u8 recv_byte;
  31   1          u8 check_sum = 0; // 存放计算之后的校验和
  32   1          u8 i;             // 循环计数值
  33   1      
  34   1          if (0 == uart0_rxbuffer_get_count())
  35   1          {
  36   2              // USER_TO_DO 未收到数据，累加超时计时
  37   2              if (timeout_cnt >= UART_DATA_HANDLE_TIMEOUT)
  38   2              {
  39   3                  // 接收超时
  40   3                  timeout_cnt = 0;
  41   3                  timeout_enable = 0;                                     // 不使能超时计数
  42   3                  uart_data_handle_status = UART_DATA_HANDLE_STATUS_IDLE; // 重新开始接收
  43   3      
  44   3                  // 打印超时之后，缓冲区内的数据
  45   3                  printf("=================================>\n");
  46   3                  printf("uart recv timeout\n");
  47   3                  for (i = 0; i < ARRAY_SIZE(cmd_buff); i++)
  48   3                  {
  49   4                      printf("%02x", (u16)cmd_buff[i]);
  50   4                  }
  51   3                  printf("=================================^\n");
C51 COMPILER V9.60.7.0   UART_DATA_HANDLE                                                  02/11/2026 10:29:10 PAGE 2   

  52   3              }
  53   2      
  54   2              return; // 串口缓冲区的数据为空，直接返回
  55   2          }
  56   1      
  57   1          timeout_enable = 1; // 使能超时计数
  58   1          timeout_cnt = 0;
  59   1          recv_byte = uart0_rxbuffer_get_byte();
  60   1          // USER_TO_DO 收到了数据，清空超时计时
  61   1      
  62   1          switch (uart_data_handle_status)
  63   1          {
  64   2          case UART_DATA_HANDLE_STATUS_IDLE:
  65   2              if (UART_DATA_HANDLE_FORMAT_HEAD == recv_byte)
  66   2              {
  67   3                  cmd_buff[0] = recv_byte;
  68   3                  cur_cmd_buff_len = 1;
  69   3      
  70   3                  uart_data_handle_status = UART_DATA_HANDLE_STATUS_FORMAT_HEAD;
  71   3              }
  72   2              break;
  73   2              // ===============================================================
  74   2          case UART_DATA_HANDLE_STATUS_FORMAT_HEAD:
  75   2              cmd_buff[cur_cmd_buff_len++] = recv_byte;
  76   2              dest_cmd_buff_len = recv_byte;                         // 存放要接收的数据长度
  77   2              uart_data_handle_status = UART_DATA_HANDLE_STATUS_LEN; // 表示接收到了数据帧长度
  78   2              // printf("len == %bu\n", dest_cmd_buff_len);
  79   2              break;
  80   2              // ===============================================================
  81   2          case UART_DATA_HANDLE_STATUS_LEN:
  82   2              cmd_buff[cur_cmd_buff_len++] = recv_byte;
  83   2              if (cur_cmd_buff_len >= dest_cmd_buff_len) // 如果接收完所有的数据
  84   2              {
  85   3                  for (i = 0; i < dest_cmd_buff_len - 1; i++)
  86   3                  {
  87   4                      check_sum += cmd_buff[i];
  88   4                  }
  89   3      
  90   3                  if (check_sum != cmd_buff[dest_cmd_buff_len - 1])
  91   3                  {
  92   4                      // 校验和错误
  93   4                      printf("check sum error\n");
  94   4                      timeout_cnt = 0;
  95   4                      timeout_enable = 0;                                     // 不使能超时计数
  96   4                      uart_data_handle_status = UART_DATA_HANDLE_STATUS_IDLE; // 重新接收数据
  97   4                  }
  98   3                  else
  99   3                  {
 100   4                      // 校验和正确
 101   4                      printf("check sum ok\n");
 102   4                      uart_data_handle_status = UART_DATA_HANDLE_STATUS_END;
 103   4                  }
 104   3              }
 105   2              break;
 106   2          // ===============================================================
 107   2          default:
 108   2      
 109   2              break;
 110   2          }
 111   1      
 112   1          if (UART_DATA_HANDLE_STATUS_END != uart_data_handle_status)
 113   1          {
C51 COMPILER V9.60.7.0   UART_DATA_HANDLE                                                  02/11/2026 10:29:10 PAGE 3   

 114   2              return; // 未接收完数据，不进入下面的处理操作，函数直接返回
 115   2          }
 116   1      
 117   1          // 打印接收到的一帧数据
 118   1          for (i = 0; i < dest_cmd_buff_len; i++)
 119   1          {
 120   2              printf("0x%02x ", (u16)cmd_buff[i]);
 121   2          }
 122   1          printf("\n");
 123   1      
 124   1          switch (cmd_buff[2])
 125   1          {
 126   2              // 判断要操作哪个电机
 127   2          case 0x01:
 128   2              // printf("obj == motor 0 \n");
 129   2              switch (cmd_buff[3])
 130   2              {
 131   3                  // 判断要执行什么操作
 132   3              case 0x00: // 停止
 133   3                  break;
 134   3              case 0x01: // 正转
 135   3                  motor_0_change_status(MOTOR_STATUS_FORWARD);
 136   3                  printf("motor 0 forward\n");
 137   3                  break;
 138   3              case 0x02: // 反转
 139   3                  motor_0_change_status(MOTOR_STATUS_REVERSE);
 140   3                  printf("motor 0 reverse\n");
 141   3                  break;
 142   3              default:
 143   3                  break;
 144   3              }
 145   2              break;
 146   2              // =======================================
 147   2          case 0x02:
 148   2              // printf("motor 1 \n");
 149   2              switch (cmd_buff[3])
 150   2              {
 151   3                  // 判断要执行什么操作
 152   3              case 0x00: // 停止
 153   3                  break;
 154   3              case 0x01: // 正转
 155   3                  motor_1_change_status(MOTOR_STATUS_FORWARD);
 156   3                  break;
 157   3              case 0x02: // 反转
 158   3                  motor_1_change_status(MOTOR_STATUS_REVERSE);
 159   3                  break;
 160   3              default:
 161   3                  break;
 162   3              }
 163   2              break;
 164   2      
 165   2          default:
 166   2              break;
 167   2          }
 168   1      
 169   1          // 处理完成后，重新接收数据
 170   1          timeout_cnt = 0;
 171   1          timeout_enable = 0; // 不使能超时计数
 172   1          uart_data_handle_status = UART_DATA_HANDLE_STATUS_IDLE;
 173   1      }


C51 COMPILER V9.60.7.0   UART_DATA_HANDLE                                                  02/11/2026 10:29:10 PAGE 4   

MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    526    ----
   CONSTANT SIZE    =    171    ----
   XDATA SIZE       =     16       2
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
